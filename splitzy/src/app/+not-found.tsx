import { NotFound } from "@/components/not-found";

export default function NotFoundScreen() {
  return <NotFound title="This page doesn't add up" message="Whatever you were looking for isn't here. Let's get you back to splitting bills." />;
}
