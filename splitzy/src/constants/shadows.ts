// The shadow attempts (single box-shadow, then a layered Material one)
// didn't read well across platforms, so list-item "cards" use a plain
// border instead — simpler and looks correct everywhere.
export const cardBorder = {
  borderWidth: 1,
  borderColor: "#e6e6e6",
};
